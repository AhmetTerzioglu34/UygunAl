@model List<Order>

<section class="panel">

    <header class="panel-heading">
        DataTables hidden row details example
    </header>

    <div class="panel-body">
        <div class="adv-table">
            <table cellpadding="0" cellspacing="0" border="0" class="display table table-bordered" id="hidden-table-info">
                <thead>
                    <tr>
                        <th>Order ID</th>
                        <th>Shipping Address</th>
                        <th class="hidden-phone">Name Description</th>
                        <th class="hidden-phone">Products</th>
                    </tr>
                </thead>

                <tbody>

                    @foreach (Order item in Model)
                    {
                        <tr class="gradeX">
                            <td>@item.ID</td>
                            <td>@item.ShippingAddress </td>
                            <td class="hidden-phone">@item.NameDescription</td>
                            <td>
                                <ul>
                                    @foreach (OrderDetail orderItem in item.OrderDetails)
                                    {
                                        <li>@orderItem.Quantity x @orderItem.Product.ProductName</li>
                                    }
                                </ul>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>
</section>
